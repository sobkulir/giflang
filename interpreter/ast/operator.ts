enum Operator {
  PLUS = '+',
  MINUS = '-',
  MUL = '*',
  DIV = '/',
  MOD = '%',
  OR = '||',
  AND = '&&',
  NOT = '!',
  LT = '<',
  LE = '<=',
  EQ = '==',
  NE = '!=',
  GE = '>=',
  GT = '>',
}

export { Operator }
